NEXT_PUBLIC_API_URL=/api-proxy
NEXT_PUBLIC_USER_ID=NEXT_PUBLIC_USER_ID
# Placeholders replaced at container start by entrypoint.sh (must match the key name
# so Next inlines the literal string into the client bundle).
NEXT_PUBLIC_GOOGLE_CLIENT_ID=NEXT_PUBLIC_GOOGLE_CLIENT_ID
NEXT_PUBLIC_AUTH_UI_REQUIRED=NEXT_PUBLIC_AUTH_UI_REQUIRED
