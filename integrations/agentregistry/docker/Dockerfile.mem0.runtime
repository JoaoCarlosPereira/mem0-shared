FROM alpine:3.20
RUN apk add --no-cache ca-certificates wget \
    && adduser -D -u 65532 nonroot
COPY docker/artifacts/mem0registry /mem0registry
EXPOSE 8080
USER nonroot
ENTRYPOINT ["/mem0registry"]
