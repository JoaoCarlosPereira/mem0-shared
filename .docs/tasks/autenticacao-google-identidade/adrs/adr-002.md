# ADR-002: NextAuth na UI com JWT de sessão emitido pela API

## Status

Aceito

## Data

2026-07-02

## Contexto

A UI (Next.js 15/React 19) não tem autenticação nem lib de auth instalada; a API (FastAPI) tampouco possui dependências de OAuth/JWT. O proxy `/api-proxy` já repassa todos os headers do browser ao backend. O `CORSMiddleware` atual usa `allow_origins=["*"]` com `allow_credentials=True` (combinação inválida para cookies de browser) e o `TeamAuthMiddleware` roda antes do CORS — soluções baseadas em cookie de sessão exigiriam reordenar middlewares e restringir origens.

## Decisão

- **NextAuth (Auth.js v5)** na UI conduz o fluxo OAuth com o provider Google e protege as rotas via `middleware.ts`.
- No callback de login, a UI envia o **ID token do Google** para a API (`POST /api/v1/auth/google`).
- A API valida o ID token com `google-auth` (assinatura, `aud`, `iss`, `exp` e claim `hd` = domínio da empresa), registra/atualiza o usuário (chave = claim `sub`) e emite um **JWT próprio de sessão**.
- A UI guarda o JWT na sessão do NextAuth e o anexa como `Authorization: Bearer` em todas as chamadas via interceptor do axios; o proxy repassa sem alteração.

## Alternativas Consideradas

### Alternativa 1: OAuth todo no backend (authlib + cookie HttpOnly)

- **Descrição**: FastAPI conduz `/auth/login` → Google → `/auth/callback`, sessão em cookie HttpOnly.
- **Prós**: Menos código na UI; segredo do OAuth só no backend.
- **Contras**: Exige reordenar middlewares (CORS externo), abandonar `allow_origins=["*"]` e gerenciar sessão server-side; cookie + wildcard CORS é inválido no browser.
- **Por que rejeitada**: Mais mudanças estruturais na cadeia de middlewares por um benefício pequeno.

### Alternativa 2: ID token do Google em toda request

- **Descrição**: Botão Google Identity Services; cada chamada envia o ID token e a API valida sempre.
- **Prós**: Sem sessão própria.
- **Contras**: Depende do refresh silencioso do Google; acopla toda request à validação externa; latência e fragilidade.
- **Por que rejeitada**: Sessão própria da API é mais robusta e barata.

## Consequências

### Positivas

- API permanece a fonte da verdade de identidade (registro, vínculo, token de agente).
- Sem cookies cross-origin: `Bearer` via proxy evita o problema CORS+credenciais.
- Rotas da UI protegidas de forma declarativa (middleware do Next).

### Negativas

- Duas camadas de sessão (NextAuth na UI + JWT da API) para manter sincronizadas (expiração alinhada).
- Novas dependências: `next-auth` na UI; `google-auth` e lib de JWT (`pyjwt`) na API.

### Riscos

- Divergência de expiração entre sessão NextAuth e JWT da API → alinhar TTLs e renovar o JWT no refresh da sessão.

## Notas de Implementação

Novas envs: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `NEXTAUTH_SECRET` (UI) e `AUTH_JWT_SECRET`, `AUTH_ALLOWED_DOMAIN` (API), seguindo o padrão de secrets do compose (`/run/secrets/...`).

## Referências

- [PRD — F1 Login com Google na UI](../_prd.md)
- Verificação de ID token: https://developers.google.com/identity/gsi/web/guides/verify-google-id-token
